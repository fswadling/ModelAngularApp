import { ProjectFormErrorsPipe } from './project-form-errors.pipe';

describe('ProjectFormErrorsPipe', () => {
  it('create an instance', () => {
    const pipe = new ProjectFormErrorsPipe();
    expect(pipe).toBeTruthy();
  });
});
