import { TotalAdditionalVolumePipe } from './total-additional-volume.pipe';

describe('TotalAdditionalVolumePipe', () => {
  it('create an instance', () => {
    const pipe = new TotalAdditionalVolumePipe();
    expect(pipe).toBeTruthy();
  });
});
